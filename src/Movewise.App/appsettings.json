{
  // Application (client) ID of your Movewise app registration. See docs/app-registration.md.
  // You can also set the MOVEWISE_CLIENT_ID environment variable instead.
  "ClientId": "",

  // Where new releases are published (see docs/releasing.md). Leave empty to turn off update checks.
  "UpdateUrl": ""
}
